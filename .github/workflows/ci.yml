name: CI

on:
  push:
    paths:
      - "main.py"
      - "requirements.txt"
      - "tests/**"
      - ".github/workflows/**"
  pull_request:
    paths:
      - "main.py"
      - "requirements.txt"
      - "tests/**"
      - ".github/workflows/**"

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install FFmpeg
        run: sudo apt-get update && sudo apt-get install -y ffmpeg
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Compile
        run: python -m py_compile main.py tests/test_assets.py
      - name: Unit tests
        run: python -m unittest discover -s tests -v
      - name: Self test
        run: python main.py --self-test
      - name: Dry-run example render
        run: python main.py --input examples/example.json --dry-run
